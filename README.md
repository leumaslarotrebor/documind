## Modes

### Lightweight Mode (Live Deployment)
- Uses keyword-based search
- Runs on low-resource environments (Render free tier)
- Accessible via deployed API

### AI Mode (Local Only)
- Uses transformer-based embeddings (Sentence Transformers)
- Enables semantic search
- Requires higher memory, runs locally only
